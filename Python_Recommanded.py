def function_avg_len(numbers):
    return print(
            f"List {list} average is {(sum(numbers)/len(numbers)):.2f} and list length is {len(numbers)}."
            ), print("--------------")


def function_square(input):
    return print(
            f"The square of {input} is {input**2:,}."
            )

list=[1,2,45,99]  

number=99

function_avg_len(list)
function_square(number)
